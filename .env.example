ANTHROPIC_API_KEY=

# Optional. If set, POST /ask requires this value in an X-API-Key header.
# Leave unset for local dev. Set it before deploying the API anywhere public --
# every request triggers a real, billed Claude API call.
ASK_API_KEY=
